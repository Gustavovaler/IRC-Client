package main

import (
	"fmt"
)

type Client struct {
}
type Connexion struct {
}
type Message struct {
}

func main() {
	protocol := "TCP"
	server := "irc.freenode.net"
	port := 6667
	channel := "#bitcoin"
	username := "Gambito123"
	msg := " :Hola !"

}
